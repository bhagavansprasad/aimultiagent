# frontend/config.py

class Config:
    # FastAPI backend URL
    BACKEND_URL = "http://localhost:8000"
    
    # Streamlit-specific configurations (e.g., title, theme)
    APP_TITLE = "Vector Database Query Interface"
    THEME = "light"
